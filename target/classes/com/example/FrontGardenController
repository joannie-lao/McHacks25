package com.example;

import java.io.IOException;

import javafx.fxml.FXML;
import javafx.scene.layout.VBox;
import java.util.List;
import javafx.scene.control.CheckBox;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import com.example.models.GameManager;
import com.example.models.Habit;

public class FrontGardenController {
    private Area area = GameManager.getInstance().getAreas().get(0);
    @FXML
    private void switchToStore() throws IOException {
        App.setRoot("store");
    }
    private void loadDecorations(){
        
    }
    
    }